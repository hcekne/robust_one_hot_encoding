# Create an instance of the encoder
source("OneHotEncoder.R")


# encoder <- OneHotEncoder()

# # Fit the encoder with the training data
# encoder$fit(training_data, cols = c("fuel_type", "color"))

# # Retrieve and print the categories
# categories <- encoder$get_categories()
# print("Categories learned:")
# print(categories)